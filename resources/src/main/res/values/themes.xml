<resources>
    <!-- Base application theme. -->
    <style name="Theme.FSPlayer" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/black</item>
        <item name="colorPrimaryVariant">@color/main_accent_color</item>
        <item name="colorOnPrimary">@color/main_accent_color</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/white</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorPrimary</item>
        <!-- Customize your theme here. -->
        <item name="android:windowBackground">@color/background_main</item>
        <item name="android:textColorPrimary">@color/main_accent_color</item>
        <item name="android:textColor">@color/main_accent_color</item>
        <item name="itemIconTint">@color/main_accent_color</item>
        <item name="colorControlNormal">@color/main_accent_color</item>
    </style>

    <style name="Toolbar" parent="Widget.AppCompat.Toolbar">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">?attr/actionBarSize</item>
        <item name="android:elevation">4dp</item>
    </style>

    <style name="FragmentContainerView">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">0dp</item>
    </style>

</resources>